{
	"name": "@ruah-dev/cli",
	"version": "1.0.2",
	"description": "CLI for ruah — multi-agent developer toolkit. Workspace isolation, file locking, DAG scheduling, merge coordination.",
	"type": "module",
	"bin": {
		"ruah": "dist/cli.js"
	},
	"files": [
		"dist/",
		"LICENSE",
		"README.md"
	],
	"scripts": {
		"build": "tsc && chmod +x dist/cli.js",
		"dev": "tsc --watch",
		"pretest": "npm run build",
		"test": "node dist/cli.js --version",
		"lint": "biome check .",
		"format": "biome check --write .",
		"typecheck": "tsc --noEmit",
		"clean": "rm -rf dist",
		"prepublishOnly": "npm run clean && npm run build && npm test"
	},
	"keywords": [
		"ai",
		"agents",
		"orchestration",
		"multi-agent",
		"claude-code",
		"codex",
		"aider",
		"cursor",
		"worktree",
		"workflow",
		"cli"
	],
	"author": "ruah-dev",
	"license": "MIT",
	"publishConfig": {
		"access": "public"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/ruah-dev/ruah-cli.git"
	},
	"bugs": {
		"url": "https://github.com/ruah-dev/ruah-cli/issues"
	},
	"homepage": "https://github.com/ruah-dev/ruah-cli#readme",
	"engines": {
		"node": ">=18.0.0"
	},
	"dependencies": {
		"@ruah-dev/orch": "^1.0.3"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.10",
		"@types/node": "^25.5.2",
		"typescript": "^6.0.2"
	}
}
